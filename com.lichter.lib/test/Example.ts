import SortFilterTable from "com/lichter/lib/SortFilterTable";

// create a new instance of the Example control and
// place it into the DOM element with the id "content"
new SortFilterTable({}).placeAt("content");
